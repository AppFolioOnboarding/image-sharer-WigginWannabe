<p id="notice"><%= notice %></p>

<%= render 'shared/image_view', url: @image.url %>

<div class='text-center'>
  <%= @image.tag_list.join(' • ') %>
</div>

<%= link_to 'Edit', edit_image_path(@image) %> |
<%= link_to 'Back', images_path %>